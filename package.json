{
  "name": "hblmweb",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=24.21.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run check",
    "preview": "astro preview",
    "astro": "astro",
    "check": "npm run check:tokens && npm run check:paths && npm run check:links && npm run check:a11y",
    "check:tokens": "node scripts/check-tokens.mjs",
    "check:paths": "node scripts/check-base-paths.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:a11y": "node scripts/check-a11y.mjs"
  },
  "dependencies": {
    "@fontsource/poppins": "^5.3.0",
    "@fontsource/work-sans": "^5.3.0",
    "astro": "^7.3.2"
  }
}
